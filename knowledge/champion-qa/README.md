# C：销冠 Q&A 目录

## 用途

资深销售整理的客户问答，**冲突时优先于 B**，但须在答复中备注 B（见 `SKILL.md`）。

## 条目格式

每个文件可含多条 Q&A，格式见 `SKILL.md` 或 `_example.md`。

## 更新流程

1. 与销冠逐条整理 Q&A  
2. 新增或更新 `.md` 文件  
3. 更新 `knowledge/META.md` 的 `qa_version`  
4. 若与 B 冲突，填写 `B_note` 字段  
